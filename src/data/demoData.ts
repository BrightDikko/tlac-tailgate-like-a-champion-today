/**
 * Compatibility re-export. Prefer `import { … } from '@/src/data/localData'` in new code.
 */
export {
  currentGame,
  currentUser,
  donationCenters,
  impact,
  menuItems,
  reviews,
  surplusItems,
  tailgates,
} from './localData';
